services:
  worker-bot:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - repo-data:/app/repo-workspace
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  repo-data:
