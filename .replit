run = "npm start"
entrypoint = "src/app.ts"

[nix]
channel = "stable-24_05"
packages = ["nodejs_20", "git"]

[env]
REPO_LOCAL_PATH = "./repo-workspace"

[deployment]
run = ["sh", "-c", "npm install && sh setup-repo.sh && node start.js"]
